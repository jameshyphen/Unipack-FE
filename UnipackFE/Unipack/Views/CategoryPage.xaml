<Page
    x:Class="Unipack.Views.CategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unipack.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Text="Categories" TextWrapping="Wrap" FontSize="48" Margin="84,58,1174,870"/>
        <ListView x:Name="ListViewCategories" Margin="84,168,83,37" Background="#FFBDBDBD" >
            <ListView.ItemTemplate>

                <DataTemplate>



                </DataTemplate>

            </ListView.ItemTemplate>
            <TextBlock Text="{Binding Name}"></TextBlock>

            <TextBlock Text="{Binding AddedOn}"></TextBlock>

            <TextBlock Text="{Binding NumberOfItems}"></TextBlock>
        </ListView>
    </Grid>

</Page>
